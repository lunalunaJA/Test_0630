<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zenithst.core.cycle.mapper.ZappCycleMapper" >

	<update  id="callDailyStatics" statementType="CALLABLE" parameterType="map" >
		{call sp_status_day(#{pCompanyid, mode=IN, jdbcType=CHAR}, #{pDate, mode=IN,  jdbcType=VARCHAR})}
	</update >

	<!--  call sp_status_day(#{pCompanyid, mode=IN, jdbcType=CHAR}, #{pDate, mode=IN,  jdbcType=VARCHAR}) -->

</mapper>